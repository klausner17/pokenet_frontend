export class Parameter {
  // tempo que será apresentado o toast
  static toastTime = 3000;

  // mensagem que será mostrado quando não for possível se conectar com o backend
  static connectBackendError = 'Servidor inacessível. Tente mais tarde';

  // mensagem que será mostrado quando há dados inválidos
  static dataError = 'Dados inválidos.';
}
